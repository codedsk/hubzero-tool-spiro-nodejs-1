            <script>
                spirograph = document.getElementById('spirograph');
                Plotly.plot(
                    spirograph,
                    [{
                        x: [<%= data.outputs.x %>],
                        y: [<%= data.outputs.y %>]
                    }],
                    {
                        margin: { t: 0 }
                    }
                );
            </script>
